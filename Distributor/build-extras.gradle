android {
    lintOptions {
        disable 'MissingTranslation'
    }

    buildTypes {
        release {
            ndk {
                abiFilters "armeabi-v7a"
	        abiFilters "arm64-v8a"
            }
        }

        debug {
            ndk {
	        abiFilters "x86"
                abiFilters "armeabi-v7a"
	        abiFilters "arm64-v8a"
            }
        }
    }

}